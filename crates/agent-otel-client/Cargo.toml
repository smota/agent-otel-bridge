[package]
name = "agent-otel-client"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Ultra-fast hook client for AI Agent CLIs (< 1ms execution)"

[[bin]]
name = "agent-hook"
path = "src/main.rs"

[dependencies]
agent-otel-ipc = { path = "../agent-otel-ipc", default-features = false, features = ["client-sync"] }
